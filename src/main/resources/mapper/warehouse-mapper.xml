<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.inventory.goods.dao.WarehouseDao">
  <select id="selectAll" resultType="kr.inventory.goods.dto.Warehouse">
    select * from tb_warehouse
    order by warehouse_no
  </select>

  <select id="selectList" resultType="kr.inventory.goods.dto.Warehouse">
    select * from tb_warehouse
    <where>
      <if test="keyword != null and keyword != ''">
        and upper(warehouse_name) like '%' || upper(#{keyword}) || '%'
      </if>
      <if test="useYn != null and useYn != ''">
        and use_yn = #{useYn}
      </if>
    </where>
    order by warehouse_no
  </select>

  <select id="selectActiveList" resultType="kr.inventory.goods.dto.Warehouse">
    select * from tb_warehouse
    where use_yn = 'Y'
    order by warehouse_no
  </select>

  <select id="selectOne" resultType="kr.inventory.goods.dto.Warehouse">
    select * from tb_warehouse
    where warehouse_no = #{warehouseNo}
  </select>

  <insert id="insert">
    insert into tb_warehouse (warehouse_no, warehouse_name, use_yn)
    values (seq_warehouse.nextval, #{warehouseName}, #{useYn})
  </insert>

  <update id="update">
    update tb_warehouse
    set warehouse_name = #{warehouseName},
        use_yn = #{useYn}
    where warehouse_no = #{warehouseNo}
  </update>

  <select id="countAll" resultType="int">
    select count(*) from tb_warehouse
  </select>
</mapper>
